- UI
    - use shadcn
    - tcp with backed
    - shwos the csv i uploaded 
    - shows the treated which is generated
    - Also does the sampling if i remple with diffrent dropout hwo much does it cahnges
    - do not need to create a database

- server
    - FastAPI (tcp)
